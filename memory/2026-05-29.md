# Memory: 2026-05-29

- Conducted a comprehensive review of the Story 5.1 specification file: `_bmad-output/implementation-artifacts/5-1-normalize-defender-context-for-melee-resolution.md`.
- Identified multiple critical gaps, inconsistencies, and bugs in the specification:
  - Discrepancy between AC 2 ("falls through to legacy") and Task 5's fixture expected behavior (structured `CombatOutcome` with `manualResolution`).
  - Missing skill item support in `target-normalizer.js` (needed for opposed rolls).
  - Incorrect assumption in Dev Notes that `normalizeSelectedTargets` is not called for melee.
  - Structural fixture crash in the proposed `melee-baseline.json` due to missing root fields (`rolls` and `expected`).
  - Validation discrepancies for martial arts skill choices and actions in the guard/resolver validation.
- Documented these findings to provide constructive feedback to the user before development begins.
- Conducted the `/bmad-code-review` workflow for Story 5.2 ("Resolve Baseline Opposed Melee Attacks"):
  - Triaged findings with developer feedback (retained safety-guards as dismissed, kept targetSnapshotCopy, and applied all agreed fixes).
  - Fixed armorPiercingEvidence generation to use rawStoppingPower and include missing penetrating fields.
  - Added Math.max(1, ...) guard for negative strength bonus damage.
  - Removed fencingKey check from defense skill fallbacks to align with AC 4.
  - Cleaned up unused helper hasSkillCaseInsensitive and unused keyTechniqueBonus variable.
  - Verified that all 8 combat fixture tests still pass.
  - Synced sprint-status.yaml and marked Story 5.2 as done.
